# FablePress Environment Configuration Example
# Copy this file to .env and fill in your values.

# DB_MODE: 'sqlite' or 'mysql'
DB_MODE=sqlite

# MySQL Database settings (Only used if DB_MODE=mysql)
DB_HOST=localhost
DB_NAME=fablepress
DB_USER=root
DB_PASS=
